node_modules/
dist/
keys/*.pem
.DS_Store
out/
mgcal-*.zip

# Calendar exports used to seed dev mode. These hold real attendee emails and
# meeting titles, so they belong outside the repo entirely (see README) — this
# is only a backstop for anything dropped in by habit.
real-events.json
*.ics
